deployer:
  serviceAccount:
    enabled: true

linter:
  serviceAccount:
    enabled: true

networking:
  networkPolicy:
    enabled: true
  podNetworkPolicy:
    enabled: true
  route:
    enabled: true
    host:
